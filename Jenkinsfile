pipeline {
 agent any
  stages {
     stage ('compile') {
     steps {
      echo "compile the code"
      }
     }
     stage ('build') {
      steps {
      echo "Building application"
      }
     }
  }
}
